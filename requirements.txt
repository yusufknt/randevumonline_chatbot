fastapi>=0.115
uvicorn[standard]>=0.30
motor>=3.5
pymongo>=4.8
redis>=6.0
pydantic>=2.7
pydantic-settings>=2.4
httpx>=0.27
cryptography>=42.0
python-dotenv>=1.0
edge-tts>=7.0

# Notlar:
# - python-dotenv: uvicorn[standard] transitif olarak getirse de explicit
#   listing tutuldu — app/core/config.py load_dotenv() doğrudan çağırıyor.
# - bson: pymongo >= 4.0 ile birlikte gelir, ayrıca listelenmez.
# - aiofiles: FastAPI StaticFiles için gerekir; static mount kaldırıldığı
#   için gerekmiyor.
# - email-validator: db/models.py'da EmailStr type-hint var ama modeller
#   hiçbir yerde instantiate edilmiyor. Pydantic doğrulaması devreye girerse
#   `pydantic[email]` eklenmeli.
# - Pillow / phonenumbers / python-dateutil: kaldırıldı (eski görsel/calendar_image
#   modülleriyle birlikte). Aktif AI (app/core/ai.py + tools.py — Groq, OpenAI-uyumlu)
#   yalnız httpx kullanır; ek paket gerektirmez.
pygame-ce==2.5.7
soxr==1.0.0
rapidfuzz==3.14.3
prometheus-client==0.23.1
faster-whisper==1.2.1
onnxruntime==1.27.0
numpy==2.5.1
